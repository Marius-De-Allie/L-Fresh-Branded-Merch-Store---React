class CartItem {
  constructor(id, name, size, price, quantity, totalPrice) {
    this.id = id;
    this.name = name;
    this.size = size;
    this.price = price;
    this.quantity = quantity;
    this.totalPrice = totalPrice
  }
};

export default CartItem;