class CartItem {
  constructor(id, name, price, quantity, totalPrice) {
    this.id = id;
    this.name = name;
    this.price = price;
    this.quantity = quantity;
    this.totalPrice = totalPrice
  }
};

export default CartItem;